# Agents

Agents represent autonomous reasoning entities.

Each agent:
- Owns its own LangGraph
- Has its own prompts
- Has its own configuration

Agents are NOT microservices.
They are logical reasoning units.
